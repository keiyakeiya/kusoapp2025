// Generated by CoffeeScript 2.7.0
export var draw_doors = function(a_doors) {
  var container, d, i, len, results;
  container = document.querySelector('#container');
  results = [];
  for (i = 0, len = a_doors.length; i < len; i++) {
    d = a_doors[i];
    results.push(container.insertAdjacentElement('beforeend', d.get_dom()));
  }
  return results;
};

export var update_message = function(a_message, a_replace = false) {
  var message_area;
  message_area = document.querySelector('#message');
  if (a_replace) {
    return message_area.innerHTML = a_message;
  } else {
    return message_area.innerHTML += a_message;
  }
};
